// import axios from './axios.min';

const ajaxLoadMore = () => {
    
    // const button = document.querySelector('.load-more');

    // if(typeof (button) != 'undefined' && button != null) {
    //     button.addEventListener('click', (e) => {

    //         let current_page = document.querySelector('.post-list').dataset.page;
    //         // let max_pages = document.querySelector('post-list').dataset.page;

    //         let params = new URLSearchParams();
    //         params.append('action', 'load_more_posts');
    //         params.append('current_page', current_page);
    //         // params.append('max_pages', max_pages);

    //         axios.post('/wp-admin/admin-ajax.php', params)
    //         .then(res => {
    //             console.log({res});
    //         })
            
    //     });
    // }

}

export default ajaxLoadMore;